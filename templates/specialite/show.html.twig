{% extends 'base.html.twig' %}

{% block title %}Specialite{% endblock %}

{% block body %}
    <h1>Specialite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specialite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ specialite.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ specialite.description }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ specialite.categorie }}</td>
            </tr>
            <tr>
                <th>SousCategorie</th>
                <td>{{ specialite.sousCategorie }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ specialite.code }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ specialite.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_specialite_index') }}">back to list</a>

    <a href="{{ path('app_specialite_edit', {'id': specialite.id}) }}">edit</a>

    {{ include('specialite/_delete_form.html.twig') }}
{% endblock %}
